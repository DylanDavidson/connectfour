// Generated by CoffeeScript 1.10.0
(function() {
  this.AI = (function() {
    AI.prototype.last = 0;

    function AI(game) {
      this.game = game;
    }

    AI.prototype.randomColumn = function() {
      return this.last = Math.round(Math.random() * 6);
    };

    AI.prototype.move = function() {
      return this.randomColumn();
    };

    AI.prototype.simulate = function() {
      var column, i, j, results, time;
      column = this.randomColumn();
      time = 0;
      results = [];
      for (i = j = 1; j <= 4; i = ++j) {
        time += 600;
        results.push(timeout(time, function() {
          this.last = column;
          this.game.highlight(column);
          if (column === 0) {
            return column += 1;
          } else if (column === 6) {
            return column -= 1;
          } else {
            if (Math.round(Math.random()) === 0) {
              return column -= 1;
            } else {
              return column += 1;
            }
          }
        }));
      }
      return results;
    };

    return AI;

  })();

}).call(this);
